// node is a run time environment for javascript
const arr = [1, 3, 4, 5, 3, 9];
console.log(arr[arr.length - 1]); // 9

// it comes a lot of pre installed library / modules
// ex.- http, fs, net, path, buffer, stream, OS, etc.
